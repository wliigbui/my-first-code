#include<stdio.h>                         //  数据溢出造成的死循环
int main() 
{
    unsigned char i = 0;              //0 <= i <= 255;
    for (i = 0; i <= 255; i++)
    {
        printf("Hello World\n");
    }
    return 0;
}
